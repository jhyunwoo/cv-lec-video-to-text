Let's quickly finish up the rest of the slides and move on to the next set of slides. Slide 8. So, talk about a few practical matters, things that you probably need to consider, but not all of them are up to date. So I'll be just kind of pointing out some things that you don't really need to pay attention to. So, comments on training algorithms, so training in general. So not guaranteed to converge to a zero training error, so you don't expect your training error to be zero all the time. May converge to a local optima. So optima means either minimum or maximum. So this can typically talk, it implies either min or max. It will oscillate indefinitely, so your loss is going to go down, but then it'll just start oscillating. It's not going to go, you know, reach zero. It may, if your data set is small enough, that means you're basically getting a perfect score on everything, but depending on what the loss is, you're not going to be able to get to zero anyways. However, in practice, it does converge to some low error for many large networks on real data with a good choice of hyperparameters, meaning you'll get something, but don't expect it to be completely zero. Thousands of epochs may require hours or days or months of training, obviously. So you see someone says, my training required 24 hours, that may not be that long. To avoid local minima problem, run several trials with random weights. So all the training that we've been talking about start from random initialized weights. So we need to start from somewhere, right? Our gradient descent needs to start from somewhere, and that somewhere depends on the random seed. So randomly, it'll be, they'll initialize with some distributions. So depending on where you start, you may be able to get better results or worse results. So with these random starts with re-weighting, test results of the trials and take results of trial with the lowest training error or use trans-learning. I'll talk about the trans-learning soon. So one of the things that are very, very common, simple mistake that you may initially make is that every time you run the model, it's going to be different. The results are going to be different a little bit. The reason why is because every time you run something, every time you initialize the weights, every time you start with some initialized weights, they're going to be so all these weights, right? They're going to be randomly initialized. So if you start one trial, it may be like 0.1, 0.2, negative 0.5. If you run it again, the same network, it'll maybe 0.3, 0.5, whatever. Depending on where you start, your end results are not going to be, chances are not going to be exactly the same. They're going to be probably different. So you're probably lucky that if you run it every time with random initialized seed. So as you know, if you've taken some other courses, you'll know that the randomness, they're deterministic in a way that if you fix the random seed, then the random number generator is going to be generating numbers with the fixed randomness. So meaning if you set, let's say some seed to be zero, that means every time you run something, the randomness is going to be fixed with the seed. That means you'll be getting the same number. So let's say I randomly sample numbers from zero to nine, one, five, four, seven, seven, nine. If I do not random initialize a seed and try to sample six numbers again, I'll be getting different numbers, whatever that number is. If I fix the seed, that means this deterministic random pattern is going to appear again. So you'll be getting one, five, four, seven, seven, nine again. Why is this important? There's always going to be randomness, but you want to fix the randomness. So every time you run the model, let's say you change the learning rate from 0.01 to 0.1. And I want to see if whichever one is actually better. Thing is, if you do not fix the random seed, then if you run the 0.01 once, trial one, if you run it again, the results are going to be different. That means if you run this with 0.01 and then 0.1, these two results are going to be different, but you're not exactly sure if it's from the randomness or it's from the change of the learning rates. It's like the typical scientific experiment. You need to control everything except for the one control variable that you have your interest, which in this case is going to be learning rate. But if the randomness is not fixed, whatever outcome that you get may be the result of this uncontrolled variable that is influencing the results. So just a very common amateur mistake. Every time you run it, you're saying you're getting different results. Chances are you're going to be getting same results or very similar results if you fix the seed. Such that when you make changes, whatever changes that you're getting, changes in terms of the results or performance, are from the changes that you're making with intention, unlike the randomness that you're not fixing. But at the same time, in practice, what's going to happen is that even if you fix the random seed, the results are going to be different because there are still uncontrollable randomness within the cutload. So that is usually okay. It's better than not fixing the seed at all. There's also different randomness that can arise. First, I talked about the weights. So the weights are going to be initialized randomly, resulting in different results, to the point where the results are varying, are different by a significant percentage. But there's also, what other randomness do we have? We also have the splitting randomness. We have the training. I randomly split into, let's say, seven to three ratios of training and testing. This is also random. So every time you do not fix this and you try to split it into two, things are going to be different, right? You're going to be getting different combinations of samples here and there every time you run the model. That means you're actually using different, slightly different distribution with training and slightly different distribution with testing to get the results, which means that your results that you're getting could be whatever up and down from the results could be from the luck that occurred from the distributions of the training and testing. It could be that your testing is much easier, consisting of the easier examples, which means that your accuracy is going to be higher. Could be that your outcome, final result is going to be lower because you just unluckily have hard examples in the test set, right? We want to be able to control that. At the same time, this is also just some interesting thing, just technicality. Suppose this rand samples some number, okay, from between zero and 10. And I have this code that's called the split, okay? That splits it into, again, seven to three ratios. Even if I fix the seed here, I fix the seed, okay? If I run this multiple times, same number, I'll be getting, let's say, the same number seven, then I'll be getting the same, let's say, combination. Let's say I have one through seven and then eight through nine, eight through 10, eight through 10 in this split, okay? But if you somehow decided to call another random function, you're just that samples another number from zero to nine, what's going to happen is that it's using one of the randomness here. So the seed, okay, so the randomness is, again, deterministic, right? Like I said, zero, one, seven, nine, seven, okay? Let's say this randomness used up this number. The split uses the rest of the numbers to make this random split. But if you have another, if you call another randomness before the split, then this rand is going to be using the next random number, okay? And the split is going to be used, going to be using the rest of them, the one after that. Then what happens to your seed, even though it has been fixed, it's not for every single individual function, okay? It basically follows the sequence. So if you really, really want to be strict about everything, maybe you can fix the seed for every single random number, but that's probably not a good idea, okay? So I just want you to realize that just fixing the seed does not necessarily mean that you're controlling and fixing the randomness no matter what you do, okay? The order matters, right? The number, the set of randomness that is outputted, that you get from the fixed seed, let's say zero, if it ends up using one of them, then the rest of them are going to be influencing the next set of code and it'll be influencing whatever comes afterwards, okay? So the randomness is something that you always, always need to be conscious of, okay? It may be a good thing, may be a bad thing, depending on how you control this. Maybe hard to set learning rate and just let the number of hidden units, so typically you're not the one who decides the number of hidden units, you're not the one who decides the number of layers, they're basically given by many prior works, just use them, don't try to make a, don't try to experiment with the different number of units, hidden units or layers. Learn rate though kind of depends on the data that you're using, it's not really architecture, this is about the training part, this is about the architecture, the model architecture part. So this you typically use whatever that's given, this may be up to you. Neural networks have fallen out of fashion in the 90s, early 20s, 2000s, but start getting better, sure. Overtraining, basically talking about overfitting, how do you avoid overfitting? This is, I talked about the validation set, right? If you
 Check them. So basically, they're divided into three sections because this is about the data itself. This is about the model architecture and the ways to control them a bit. These are about just the learning rates and things that are not specific to the model. So these are applied to all the models. These are model specific. These are data specific. So these are just three kind of independent components that you always have to consider. What are the regularizations? Dropouts. OK, let's talk about dropouts. So standard neural networks, you just connect everything. You always use everything. There's a thing called the dropout. It used to be popular. I'm not sure how often it's used these days. What happens is that you'll be randomly turning off some neurons. So in other words, every time you have, let's say, a mini-batch, you have 16 examples. Now I'm going to be just feeding this through and then get some results for training. What happens is on every layer, there's a probability that some of the layers, the neurons are just not, they just turn them off. So that means whatever the edges, whatever the networks that are being connected to them are going to disappear and you're not going to use them. So in this case, you can kind of expect the probability of dropping the first layer is approximately, let's say, 40 percent. So with 40 percent chance, you'll be dropping some of these layers. So maybe out of five, you drop two. Out of five, maybe you drop six. I'm guessing it's approximately 40 to 60 percent. Out of five, you drop two. So every time you let something through, OK, I'm talking about training. OK, I'm talking about training. You intentionally cut them off. So in other words, you're actually asking the model to rely on some randomly chosen subset of the hidden neurons to be able to perform as well. So and then if you have another set of mini-batch, you run this again for training. Maybe these are turned on. Maybe these are turned off. Maybe these are turned off again, but maybe these are turned off. So every time you have a new combination, what this forces the model to do is to not rely on specific. So it does not actually allow the model to fixate on some of the hidden neurons. It may actually favor if you just use everything. It may actually favor just a few paths or a few neurons. What this does is that you do not actually have to let the model to be fixated on some of the neurons. You let it actually try other combinations. This is all about the generalizability. Generalizability, regularization. So this is another form of regularization because you are forcing the weights to be evenly learned. You're not actually focusing on some of them to be getting too much of an attention. So what you allow is giving everything kind of like a random chance of being used and being focused on. So the curve you're seeing on the right. So this is the number of weights updated. So this probably means epoch iteration. Classification error, the lower is better. So without the dropout, the error stays at around 0.7%, 1.7%. But with the dropout, it actually falls nearly down to 1%. And this is one of the most influential works. It's so simple. You're just randomly turning them off. But this actually allows the model to be evenly considered. All the hidden neurons and weights to be evenly considered with some probability. Data augmentation. Extremely useful. So basically you're augmenting the data. You are changing the data without changing the semantics. That's kind of the point. You want to create virtual examples, virtual training examples. But when I say virtual, I'm not talking about like completely new. I'm talking about some reasonable geometric changes. Horizontal flip. Just because you flip the image does not necessarily mean you change the semantics underlying meaning of the object. So the goal is to be able to classify the image. So it's an image classification. You play all these things, but you know that they are all the same animals. The classification results should not change just because you rotate them, you crop some parts, you change some colors, you share them, shift them, all these affine transformations. Just because you have done some of those transformations, this should not be changing the results. So what's happening is that you are actually randomly augmenting these images without harming the underlying class. And this has the effect of boosting or amplifying the number of examples in the training set. So some combinations. You basically have some, typically you have a range, let's say a foot, you have a 50% of flipping. You have some random crops, you have some randomness of choosing the center and the size of the square of the crop or mask. Color casting, geometric distortion. So you have some rotation. The rotation angles are usually, you have a range of rotation angles, let's say negative 30 to positive 30. So you randomly sample some rotation angle and rotate it. All of them can be applied together, resulting in this sort of effect. Very important. Without this, you cannot expect to overcome overfitting in almost all cases. You're basically forcing the model to see a lot of variations of the same image. So if you have 100 images, whatever these animals, this directly can give you infinitely many samples. Because you're picking from some continuous range of interval of numbers, all of these are going to give you many, many more combinations. And you're doing this randomly every single time. Basically to the point, you don't actually use the original image. So if this is the original image, you're almost always going to be applying some sort of data augmentation and never use the original image again. Almost the end, the transfer learning. More important than ever, especially these days, you don't even use the term transfer learning because you just do it without realizing. It's just such a natural procedure that you do these days that you often forget where it came from. So talk about the transfer learning. The more weights you need to learn, the more data you need, obviously. And that's why with the deeper networks, you need more data. One possible solution. Set these. So if you have a whole network, set a huge part of it to be something that you already learned from some another network or some larger data set. And if you have a new data set, you only train the ones that are close. It's a subset of the networks. That means the amount of the weights that you're updating with your small data is actually relatively small because everything else has been pre-trained or publicly trained on something else. So transfer learning with a CNS. Suppose the source is a classification on ImageNet and the target is some whatever other task you want to do. So it's the input all the way down to the output. So what you do is train everything on ImageNet. Like I said, people have trained this and then they'll be releasing the trained model, what we call the pre-trained model. So all these weights are trained on ImageNet for ImageNet classification. And then if you have a very small data set, let's say, so I do medical imaging. So if you have some medical imaging data set, so ImageNet has, as you know, has thousands of examples. Medical image may be hundreds of examples. That means I don't want to actually train the entire model with only hundreds of examples. I'll be what we call the freezing them. So if you see papers, you'll see like snowflakes. I can't draw snowflakes, but if you see snowflakes, meanings, the model hasn't frozen means that you don't actually change them. So these you can actually force these ways to not be changed. You don't actually have to update them. So in other words, for these parts, your W is not going to be updated by W, eta, whatever the gradient of the error. There's no update only this part. Why do I only train this part? Because I only have a small subset of, I only have a very small data set. So I'm not, should probably not be attempting to train the entire big model like this. I'm only going to be changing a very, very small subset at the end, a few layers at the end. Why only a few layers at the end? As you know, there's a low level, even though it's been upside down, there's a low level, it's a high level features. And you saw the low level features are not really domain specific. They're like edges, blobs, and corners and stuff, right? So we're actually doing is this part. So early layers are mostly about just kind of talking about capturing the small level details, a low level details of the image. And as you go down, there's going to be more and more domain specific or target specific, your goal specific. So originally it's been about classifying, let's say just natural images. But what we know from this point, let's say you want to be able to classify some, let's say Alzheimer's disease versus the healthy people. Then these may actually be pretty useful because the brain images, let's say they still have some features. They have edges, circles, whatever. So we'll be using them. Eventually we need to start transitioning using these features. Think of this as the feature extractors. They're just basically like SIFT features. It's basically like SIFT features except it's based on homegrown networks. You have a very, very nice features. And in the end, I'm only going to be using them for brain disease classification. If the data set is, you have a reasonably sized amount of data set, then you may actually try to. So ideally you want to train everything. Theoretically speaking, but if you have more data set, let's say instead of hundreds, maybe you have thousands, then maybe you can try to learn more and more layers. So transfer learning is basically you train on one data set and then just try to, what we call the fine tune, right? So you're
 with the pre-training process. Train the network to classify and use that class as a last error and try and train layers to replace them. Basically, it just means that this is the original task and then you replace this with some other layers to identify some other target tasks or target labels. Transfer learning with CNNs is pervasive. So transfer learning. I'm going to talk about the object detection. So this is, I think we'll talk about this later on. Many times you'll be seeing, starting from this point, a group of researchers have pre-trained this ImageNet, typically ResNet, with the pre-trained ResNet with the ImageNet and the people just started using them. So also this, whenever you see this sort of a thing, in many cases you can expect them to be pre-trained models. Why is it here? Semantic segmentation. So you know the segmentation is kind of on the topic. Segmentation, you know, with the KNN, you know, it can be segmentation, but semantic segmentation means you are actually going to classify each pixel. So whatever each pixel is going to be classifying, whatever that pixel represents. So standard pixel, based off of the nearby context, I believe it's a cop. So I'm going to be doing this for every single pixel, meaning I'm doing the segmentation, but it has semantics. I know what that is. It's not just grouping similar things. I'm actually, almost like a classification at the pixel level. This is much more useful, much more commonly used. So often we just shorten it for segmentation, but most of the cases, whenever you're able to label each pixel, we call them semantic segmentation. This is actually a pretty significant difference. So whenever you see a task where you, seems like it's segmenting something, but also is actually classifying what that is, it's a semantic segmentation. Talking about segmentation, this is the second slide talking about segmentation. We do a lot of segmentation, surprisingly, advanced from medical imaging, because in medical imaging like this, it's an MRI, a specific type of MRI, where you're able to see this little mark, right? This is called the white matter hyperintensity. It's a small microvascular disease. Basically, it's like a lesion. It's slightly damaged, it's very minor damage to your microvessels. Okay, and why is segmentation useful in medical imaging? So it's not, I mean, diagnosis is important. So I want to be able to classify whether the person of this brain is, let's say, Alzheimer's or progressing into Alzheimer's or has a mild cognitive impairment and such. But often cases, you care about the sizes of these, what we call biomarkers. Okay, I want to be able to check the size of this. Depending on the size of this, maybe one, what I can do is, oh, this size is large. This can be like a good indicator of whatever related diseases. So segmentation actually was much more important. It's still very important in medical imaging. And the most commonly used architecture, let's call it unit. So whenever you see a segmentation, you're going to be seeing this word unit a lot, basically 99% of the time. So why is it called unit? Because it shapes like a unit. It's old, but still very well used segmentation models. So whenever you see a unit, that means you can expect some sort of a segmentation problem. Okay, so in the end, what happens is the input goes in, the output is actually a segmentation map. So this should be the output that it should be targeting. So this goes in, the output should be this sort of a map. Okay, saying, oh, these pixels are what I've been trained to highlight. One thing that you notice is, there are a few things that you may notice. There's no fully connected layers, right? Because image goes in, image comes out. Why is there a fully connected layer? So you typically try to accomplish some multi-class classification. The output typically is like a one-hot vector. In this case, it's not. It's an image. So image goes in, image goes out. So fully connected layer is not used in this case. So see these arrows and labels? So it says conv3x3 rather, copy and crop, maxpool, upconv, upconv, 1x1, so 1x1 convolution. So these are all convolutions. There's no, or convolutions or pooling. There's no zero fully connected layer. There's zero FC layers. So this originated from what we call the fully convolutional neural network. So not fully connected, but fully convolutional neural network, which means the image only has convolutions, in which case they're typically outputting the 2D of something. So unit is, I guess, originally, it's a family of fully convolutional neural network, meaning it only consists of convolution-related operations. And you see this. You see these random arrows connecting and then skipping parts. This is almost like a skip connection. So this is called a skip connect. It's from the ResNet skip connect. So whenever you see an arrow jumping here and there across the layers, it's some sort of a skip, skip this, skip that. So what this tells you is it actually doesn't really add anything. It actually concatenates. So this last thing is going to be added next to it, and then something's going to happen. So it's actually using the prior information in the later part of the neural network. So if you think about the layer, this is like a layer, the layers actually increase this way, except it kind of takes a weird path. So one another way to draw this may be like this. So it used to be drawn, I'm not going to draw every small one of them. It used to be drawn like this. Let's say it gets larger and larger and connects this way. Another way to draw that is pulling obviously makes the image smaller and smaller. One, two, three, four, five, six, okay. You can just think of it as this. There's a different way of drawing this. It's a little more convenient to maybe draw it that way, but this is actually another way to draw. So you make your line, the layers, so that it looks more familiar, it's more familiar. And then you draw the skip connection over the layers. So these two are identical representations. So there's nothing really special about the shape. It's just drawn this way, and the authors named it like that, and then we just started calling this a new unit. Power for identification, sure, it's one of the tasks. I'm just going to skip. Packages, this and that, let me skip a few things. Understanding CNNs, sure, sure, sure, let's skip, skip, skip. Okay. Oh, I'll probably end it with this. So breaking CNNs. You may have heard the word adversarial. Adversarial means doing something bad against you. So people start trying to exploit the weaknesses of CNNs. So this is a school bus. This has been correctly classified as school bus, has been classified as, I'm not sure what this, or whatever this bird is. And then this is whatever this building or thing is. What you see in the middle is a distortion. You're basically adding a small bit of a pixel-level distortion or noise to the model. And this is the resulting outcome of the image. Are you able to tell the difference? No, because the noises are so subtle, you are not able to tell the difference. I mean, you kind of can. In this case, you see a little bit of a noise here, maybe. But in most cases, you're not able to recognize these very small changes. These values are actually very small as well, and they're very sparse. And it happens over complex regions of the image, so you're also not able to tell. If it's over a pretty flat region of the image, maybe you're able to tell. But typically, it's very hard to tell. So the image has been distorted, manipulated. Now, everything has been classified. All these images are classified as ostrich. Everything here has also been classified as ostrich. So what happens is, what's actually happening is that you're actually trying to fool the linear classifier. So what happens, typically, how we update the weights. So we update, so this is called the adversarial perturbation. So you perturb the image a little bit in an adversarial way, in a bad way, so that the classification actually fails. How does that happen? I'm only going to talk about it very briefly. Typically, you update the weight like this, right? In which direction? In a way that minimizes the error, right? So you're updating the weights. What you can do, and there are two things that I want you to realize. One, you can actually update the image. So at the end of the model, you have the image, right? So you update, update, update all the weights, but then you can also update the image, okay? As shown here. I'm going to change the image such that it actually, let's say, minimizes the error. That means I'm going to change this pixel-level image details are actually going to be considered as some sort of a learnable variables. It can be updated, okay? So you're just taking another level down. So just updating the weights, I'm also going to be updating the image pixel levels, okay? Another thing you can do is instead of updating it in a way that minimizes the error, you can actually update it to increase the error, but not just increasing the error, okay? You're basically not going down, you're going up. But it's a little bit different. What you're trying to do is update it such that it's actually classifying as something else. So if you have one hot vector, let's say, somewhere in the middle is a cat, okay? And then somewhere else is an ostrich. Typically, this is our ground truth, and I want to minimize the error
 سخنران
